# Build outputs (tools\build.cmd, tools\build-isotest.cmd, tools\selftest.cmd).
x64/
tools/_build/
tools/__pycache__/
*.obj
*.pdb
*.ilk
*.exp
*.lib
*.tlog
*.log
*.wav

# Machine-specific state written by tools\session.ps1 -Prepare: boot configuration export,
# Bluetooth baseline, backup of the stock driver package, exported test certificate.
recovery/

# Certificates and keys.
*.cer
*.pfx

# Qualcomm controller firmware. Copied from the local DriverStore by tools\stage-firmware.ps1
# at build time; proprietary, never committed.
*.tlv
hpnv*

# EWDK images.
*.iso
# Local notes.
docs/ANTI_PATTERNS.md
*.local.md
