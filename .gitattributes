# Hash-checked and byte-compared records (tools\check-isoc-reference.cmd, tools\check-reference.cmd):
# stored and checked out byte for byte, whatever the local line-ending settings.
reference/** -text

# cmd.exe mis-parses labels and goto in LF-only batch files.
*.cmd text eol=crlf
